import React from 'react';
import ReactDOM from 'react-dom';

const Header = () => {
    return (
        <header>
            <h1>Alpaca Space Story</h1>
        </header>
    );
}

export default Header;
